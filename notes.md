 // const fieldRegex = /(\w+)\s+(\w+)([^@]*)((?:@\w+(?:\([^)]*\))?)*)/g;
        const fieldRegex = /(\w+)\s+([\w\[\]?]+)([^@]*)((?:@\w+(?:\([^)]*\))?)*)/g;
        // const fieldRegex = /(\w+)\s+(\w+)/g;